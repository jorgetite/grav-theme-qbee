extends@: default

form:
  fields:
    tabs:
      fields:
        buttons:
          fields:
            columns:
              fields:
                column1:
                  fields:
                    header.onpage_menu:
                      type: toggle
                      label: Single Page Menu
                      help: Sets whether the menu is based on single page or multiple page.
                      highlight: 0
                      default: 0
                      options:
                        1: PLUGIN_ADMIN.ENABLED
                        0: PLUGIN_ADMIN.DISABLED
                      validate:
                        type: bool
