{% include 'partials/fragments/profile.html.twig' 
    with {
        id: page.header.module_id,
        classes: ['text-section', page.header.module_padding]|join(' '),
        row_clss: 'auto'
    } 
%}