{% include 'partials/fragments/text.html.twig' 
    with {
        id: page.header.modular_id,
        classes: ['text-section', page.header.modular_padding]|join(' '),
        row_clss: 'auto'
    } 
%}