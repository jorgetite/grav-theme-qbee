{% include 'partials/fragments/text.html.twig' 
    with {
        id: page.header.module_id,
        classes: ['text-section', page.header.module_padding, page.header.module_classes]|join(' '),
        row_clss: 'auto'
    } 
%}