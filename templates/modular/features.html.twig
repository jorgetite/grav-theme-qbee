{% include 'partials/fragments/features.html.twig' 
    with {
        id: page.header.modular_id,
        classes: ['features-section', page.header.modular_padding]|join(' '),
        row_clss: 'auto'
    } 
%}